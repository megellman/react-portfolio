import React from 'react';

function Header() {
    return (
        <div className='fixed-top' style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>

        </div>
    );
}

export default Header;